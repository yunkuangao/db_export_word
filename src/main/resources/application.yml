server:
  port: 8080
  servlet:
    context-path: /
spring:
  application:
    name: db-export

word:
  model:
    export: D://export.docx
    import: classpath:docx/import.docx
    sub-model: classpath:docx/sub_model.docx